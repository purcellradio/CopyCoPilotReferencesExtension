Do not change formatting of existing code or remove existing comments unless the code is being changed.

Leave existing variable names as they are unless they conflict with new code being added.

When creating new variables, use descriptive names that clearly indicate their purpose and the underlying type.

When adding new code, follow the existing coding style and conventions used in the surrounding code.
